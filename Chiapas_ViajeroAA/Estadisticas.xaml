<Window x:Class="Pagina_Principal.Estadisticas"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
        xmlns:local="clr-namespace:Pagina_Principal"
        mc:Ignorable="d"
        Title="Estadisticas" Height="450" Width="800">
    <Grid Margin="10">
        <StackPanel>
            <TextBlock Text="Estadísticas Generales" FontSize="24" FontWeight="Bold" Margin="0 0 0 20" />

            <StackPanel Orientation="Horizontal" Margin="0 0 0 20">
                <TextBlock Text="Total Operadoras: " FontWeight="Bold" />
                <TextBlock x:Name="TxtTotalOperadoras" Margin="10,0" />
                <TextBlock Text="Total Administradores: " FontWeight="Bold" Margin="20,0,0,0" />
                <TextBlock x:Name="TxtTotalAdministradores" Margin="10,0" />
                <TextBlock Text="Top Operadora: " FontWeight="Bold" Margin="20,0,0,0" />
                <TextBlock x:Name="TxtTopOperadora" Margin="10,0" />
            </StackPanel>

            <lvc:CartesianChart Series="{Binding SeriesCollection}" LegendLocation="Right" Height="300">
                <lvc:CartesianChart.AxisX>
                    <lvc:Axis Title="Operadoras" Labels="{Binding Labels}"></lvc:Axis>
                </lvc:CartesianChart.AxisX>
                <lvc:CartesianChart.AxisY>
                    <lvc:Axis Title="Reservaciones"></lvc:Axis>
                </lvc:CartesianChart.AxisY>
            </lvc:CartesianChart>
        </StackPanel>
    </Grid>
</Window>
